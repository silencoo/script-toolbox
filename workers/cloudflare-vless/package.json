{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.mjs",
    "validate": "wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
