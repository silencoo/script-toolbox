{
  "name": "mcp-store-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test worker.test.mjs worker.runtime.test.mjs",
    "types": "wrangler types --include-runtime false",
    "validate": "npm run test && npm run types && wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
