// Ink's optional DEV-only module is statically visible to bundlers. Production
// builds never call this stub because process.env.DEV is compiled to "false".
export default {
  initialize() {},
  connectToDevTools() {}
};
